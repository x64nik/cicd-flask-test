version: 2.1

jobs:
  test-build-01:
    docker:
      - image:  cimg/base:stable

    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo 'python3 saying hello'" 

      - run:
          name: "test command 01"
          command: "pwd"

      - run:
          name: "test command 02"
          command: "whoami"

      - run:
          name: "test command 03"
          command: "uname -a"

      - run:
          name: "check python3"
          command: "which python3 && python3 --version"

      # - run: |
      #     which python
      #     which python3
      #     python3 --version



# Orchestrate jobs using workflows
# See: https://circleci.com/docs/configuration-reference/#workflows
workflows:
  test-build-01-workflow:
    jobs:
      - test-build-01